<%= form_with(model: event, local: true) do |form| %>
  <% if event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% event.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<label class="control-label" for="">入荷予定日</label>
  <%= form.date_select :date, {}, class: 'form-control' %>

    <div class="field">
        <%= form.label :location %>
        <%= form.text_field :location %>
    </div>
    
    <div class="field">
        <%= form.label :memo %>
        <%= form.text_field :memo %>
    </div>
    
    <div class="field">
        <%= form.label :event_type %>
        <%= form.radio_button :event_type, :practice, {:checked => true} %>
        <%= form.radio_button :event_type, :tournament %>
        
        <input type="radio" value="event_practice" name="test" id="event_event_type_tournament">


        <div class="radio-inline">
    <%= form.radio_button :event_type, :一般の求人, checked: "checked" %>
    <%= form.label :一般の求人, class: "job_type_label" %>
  </div>
  <div class="radio-inline">
    <%= form.radio_button :event_type, :特殊な求人 %>
    <%= form.label :特殊な求人, class: "job_type_label" %>
  </div>

    </div>

    <div class="actions">
        <%= form.submit %>
    </div>

<% end %>

